services:
  # FastAPI backend
  - type: web
    name: urbannomadapi
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      # Pin Python — Render defaults to 3.14 which doesn't yet have
      # prebuilt pydantic-core wheels, forcing a slow + flaky Rust build.
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: TICKETMASTER_API_KEY
        sync: false
      - key: EVENTBRITE_TOKEN
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: ADMIN_TOKEN
        sync: false
      # Neon Postgres connection string — set in the Render dashboard (it
      # contains a password, so it's a secret, not committed). If unset, the
      # app falls back to local SQLite (ephemeral on Render's free disk).
      - key: DATABASE_URL
        sync: false
      - key: CORS_ORIGINS
        value: https://vagarivita.onrender.com

  # React PWA (static site — always free, no plan needed)
  - type: web
    name: vagarivita
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://urbannomadapi.onrender.com/api
